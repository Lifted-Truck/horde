# 2026-09-05 — Comb singleton guard, B95 module policy, priority tracks

**What changed.** The human loaded a second Comb slot and the audio blew up.
Mechanism (fx_rack.h): the Comb's eight KS lines are ONE rack-owned bank and
every Comb slot iterates it — a second slot double-writes each line per block
and advances its write pointer twice; feedback compounds past unity, tuning
halves. Fix, structural: `kSlotMaxInstances` declared in the rack (Comb = 1,
everything else unlimited — they own per-slot state), `typeAllowed()`
refused at the shell's one type-write choke point (GUI, host, preset, morph
all see the refusal because readback reports the rack); GUI greys the option
in other slots ("Comb · in FX1"). `combguard_check` GREEN: refusal, audio
bit-identical to one Comb after the refusal, bounded, order-independent,
non-singletons still repeat. ROADMAP: PRIORITY TRACKS section (the human's
seven), B95 (FX final form: bounded-pool ruling with the three locking
constraints), B96 (mapping shortcuts), B97 (morph lab comes home). Roadmap
map artifact rendered from those statuses.

**Verify.** `./verify fast` exit 0 (gen_gui_controls regenerated after the
GUI edit); lab_load 26/0; combguard_check standalone GREEN (unwired, standing
ruling).
